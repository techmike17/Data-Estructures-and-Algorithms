#include <iostream>
#include <list>
#include <algorithm>
#include <string>
#include <map>
#include <vector>
#include <sstream>

using namespace std;
// double linked list with struct or whatever but using classes
int main()
{
    int nLines;
    cin >> nLines;
    while (nLines != 0)
    {
        vector<int> nums;
        for (int i = 0; i < nLines; i++)
        {
        }
        cin >> nLines;
    }
    return 0;
}